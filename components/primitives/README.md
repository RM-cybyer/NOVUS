# Component Primitives

Primitive components are the lowest-level reusable UI building blocks.

## Responsibility

- Buttons, inputs, labels, panels and other base UI elements after a UI stack is selected.
- Accessibility and interaction defaults shared across the product.

## Rules

- Keep primitives domain-agnostic.
- Do not import application services.
- Do not encode product-specific workflows.

