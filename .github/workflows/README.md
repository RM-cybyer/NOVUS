# Workflows

This folder contains GitHub Actions workflows for repository automation.

## Current Scope

The current workflow validates repository foundation structure only. It does not install dependencies, build an app or run product tests because no runtime exists yet.

## Future Scope

Add runtime-specific workflows only after the framework, package manager and deployment architecture are documented.

