# Workflows

This folder contains GitHub Actions workflows for repository automation.

## Current Scope

The current workflow validates repository foundation structure and required architecture documentation only. It does not install dependencies, build an app or run product tests because package metadata and application code do not exist yet.

## Future Scope

Add runtime-specific workflows after the framework is initialized and package scripts exist.
