# Module Specifications

Founder-issued module specs (PRD + acceptance criteria) that drive implementation milestones. One file per module; update the spec here instead of re-prompting - these documents are executable context.

- Owning AI agent: Fable (single-owner mode per Master Operating System v3.0).
- What belongs here: module PRDs, acceptance criteria, section breakdowns, QA checklists.
- What never belongs here: implementation code, secrets, chat transcripts.
