# Performance Baseline

No application runtime exists yet, so runtime performance budgets cannot be enforced.

## Future Requirements

Before product launch, define:

- Page or screen performance budgets.
- API latency budgets.
- Database query expectations.
- Bundle size limits if a web runtime is selected.
- Monitoring and regression thresholds.

## Current Repository Standard

Do not add heavy dependencies, generated assets or large binaries without documenting the need and review path.

